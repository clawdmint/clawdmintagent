// Clawdmint Database Schema
// Platform for AI agent NFT deployments

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ═══════════════════════════════════════════════════════════════════════
// AGENT MANAGEMENT
// ═══════════════════════════════════════════════════════════════════════

// AgentStatus values: PENDING, CLAIMED, VERIFIED, SUSPENDED, BANNED

model Agent {
  id            String        @id @default(cuid())
  name          String
  eoa           String        @unique // Ethereum address (checksummed)
  description   String?
  avatarUrl     String?
  xHandle       String?       // Twitter/X handle (optional)
  solanaWalletAddress String? @unique
  solanaWalletEncryptedKey String?
  solanaWalletExportedAt DateTime?
  
  // Authentication
  hmacKeyHash   String?       // Hashed HMAC key for API authentication
  
  // Notifications
  telegramChatId String?      // Telegram chat ID for mint notifications
  
  // Verification status (PENDING, CLAIMED, VERIFIED, SUSPENDED, BANNED)
  status        String        @default("PENDING")
  deployEnabled Boolean       @default(false)
  
  // Timestamps
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  verifiedAt    DateTime?
  
  // Relations
  claims        AgentClaim[]
  collections   Collection[]
  @@index([eoa])
  @@index([status])
}

// ═══════════════════════════════════════════════════════════════════════
// VERIFICATION CLAIMS
// ═══════════════════════════════════════════════════════════════════════

// ClaimStatus values: PENDING, VERIFIED, EXPIRED, FAILED

model AgentClaim {
  id          String      @id @default(cuid())
  agentId     String
  agent       Agent       @relation(fields: [agentId], references: [id], onDelete: Cascade)
  
  // Claim details
  claimCode   String      @unique // e.g., CLAWDMINT-AGENT-9K2F
  
  // Verification data
  signature   String?     // EIP-191 signature of claim code
  tweetUrl    String?     // Optional Twitter verification
  tweetId     String?     // Tweet ID for API lookup
  
  // Status (PENDING, VERIFIED, EXPIRED, FAILED)
  status      String      @default("PENDING")
  expiresAt   DateTime
  verifiedAt  DateTime?
  
  // Timestamps
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  
  @@index([claimCode])
  @@index([agentId])
}

// ═══════════════════════════════════════════════════════════════════════
// NFT COLLECTIONS
// ═══════════════════════════════════════════════════════════════════════

// CollectionStatus values: DEPLOYING, ACTIVE, PAUSED, SOLD_OUT, FAILED

model Collection {
  id              String    @id @default(cuid())
  
  // On-chain data
  address         String    @unique // Contract or program-derived collection address
  deployTxHash    String    // Deployment transaction hash or Solana signature
  chain           String    @default("base") // base, base-sepolia, solana, solana-devnet
  authorityAddress String?
  
  // Agent relation
  agentId         String
  agent           Agent     @relation(fields: [agentId], references: [id])
  agentEoa        String    // Denormalized for quick access
  
  // Collection metadata
  name            String
  symbol          String
  description     String?
  imageUrl        String?   // Collection cover image
  baseUri         String    // IPFS base URI
  mintEngine      String    @default("legacy_solana_program") // legacy_solana_program, metaplex_core_candy_machine
  mintAddress     String?   // Candy Machine address for Metaplex-powered mints
  
  // Mint parameters
  maxSupply       Int
  mintPrice       String    // Wei amount as string (bigint)
  royaltyBps      Int       // Royalty in basis points
  payoutAddress   String
  
  // Bags-native community layer
  bagsStatus      String    @default("DISABLED") // DISABLED, CONFIGURED, PREPARED, LIVE
  bagsTokenAddress String?
  bagsTokenName   String?
  bagsTokenSymbol String?
  bagsTokenMetadata String?
  bagsLaunchTxHash String?
  bagsConfigKey   String?
  bagsMintAccess  String    @default("public") // public, bags_balance
  bagsMinTokenBalance String?
  bagsFeeConfig   String?   // JSON - creator/community/referral fee share config
  bagsCreatorWallet String?
  bagsInitialBuyLamports String?
  bagsScore       Float     @default(0)
  bagsLifetimeFees String?
  bagsClaimedFees String?
  bagsAnalyticsUpdatedAt DateTime?
  
  // Mint tracking
  totalMinted     Int       @default(0)
  
  // Status (DEPLOYING, ACTIVE, PAUSED, SOLD_OUT, FAILED)
  status          String    @default("DEPLOYING")
  
  // Timestamps
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  deployedAt      DateTime?
  
  // Relations
  mints           Mint[]
  mintIntents     MintIntent[]
  
  @@index([agentId])
  @@index([address])
  @@index([chain])
  @@index([status])
  @@index([bagsStatus])
  @@index([bagsTokenAddress])
}

// ═══════════════════════════════════════════════════════════════════════
// MINT TRACKING
// ═══════════════════════════════════════════════════════════════════════

model Mint {
  id            String     @id @default(cuid())
  
  // Collection relation
  collectionId  String
  collection    Collection @relation(fields: [collectionId], references: [id])
  
  // Mint details
  minterAddress String
  quantity      Int
  totalPaid     String     // Wei amount as string
  txHash        String     @unique
  
  // Token IDs
  startTokenId  Int
  endTokenId    Int
  assetAddresses String?   // JSON array of minted Metaplex asset addresses
  
  // Timestamps
  createdAt     DateTime   @default(now())
  mintedAt      DateTime   // Block timestamp
  
  @@index([collectionId])
  @@index([minterAddress])
  @@index([txHash])
}

model MintIntent {
  id             String     @id @default(cuid())

  // Collection relation
  collectionId   String
  collection     Collection @relation(fields: [collectionId], references: [id], onDelete: Cascade)

  // Intent details
  walletAddress  String
  quantity       Int
  totalPaid      String
  assetAddresses String     // JSON array of expected Metaplex asset addresses

  // Lifecycle
  expiresAt      DateTime
  consumedAt     DateTime?
  txHash         String?

  // Timestamps
  createdAt      DateTime   @default(now())
  updatedAt      DateTime   @updatedAt

  @@index([collectionId])
  @@index([walletAddress])
  @@index([expiresAt])
  @@index([txHash])
}

// ═══════════════════════════════════════════════════════════════════════
// AGENT CHAT (Live Chat on Collection Pages)
// ═══════════════════════════════════════════════════════════════════════

model ChatMessage {
  id              String    @id @default(cuid())
  
  // Which collection page this message belongs to
  collectionId    String
  
  // Sender info
  senderType      String    // "agent" or "user"
  senderAddress   String?   // Wallet address for users, EOA for agents
  senderName      String    // Agent name or truncated wallet
  
  // Message content
  content         String
  
  // Timestamps
  createdAt       DateTime  @default(now())
  
  @@index([collectionId, createdAt])
  @@index([senderAddress])
}

// ═══════════════════════════════════════════════════════════════════════
// REPLAY PROTECTION
// ═══════════════════════════════════════════════════════════════════════

model UsedNonce {
  id        String   @id @default(cuid())
  agentId   String
  nonce     String
  usedAt    DateTime @default(now())
  
  @@unique([agentId, nonce])
  @@index([agentId])
}

// ═══════════════════════════════════════════════════════════════════════
// WHITELIST ENTRIES
// ═══════════════════════════════════════════════════════════════════════

model WhitelistEntry {
  id              String   @id @default(cuid())
  walletAddress   String   @unique
  twitterHandle   String?
  completedTasks  String   // JSON array of completed task IDs
  createdAt       DateTime @default(now())
  
  @@index([walletAddress])
}

// ═══════════════════════════════════════════════════════════════════════
// TOKEN LAUNCHES (via Bankr Partner API)
// ═══════════════════════════════════════════════════════════════════════

model TokenLaunch {
  id              String    @id @default(cuid())
  
  tokenName       String
  tokenSymbol     String
  tokenAddress    String    @unique
  poolId          String?
  txHash          String?
  
  description     String?
  imageUrl        String?
  websiteUrl      String?
  tweetUrl        String?
  chain           String    @default("base")
  
  launcherAddress String
  feeRecipient    String
  feeDistribution String?   // JSON — fee breakdown from Bankr
  
  simulated       Boolean   @default(false)
  
  createdAt       DateTime  @default(now())
  
  @@index([launcherAddress])
  @@index([tokenAddress])
  @@index([createdAt])
}
